logging:
  level:
    org.springframework.security: TRACE
    org.springframework.security.oauth2: TRACE

spring:
  application:
    name: authorization-service

  mail:
    host: smtp.gmail.com
    port: 587
    username: sservex@gmail.com
    password: vxpvrjpmomawoddl
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  cloud:
    config:
      username: ${CFG_USER}
      password: ${CFG_PASSWORD}
      uri: http://localhost:8081
      fail-fast: true
      enabled: true
      retry:
        initial-interval: 1000
        max-interval: 2000
        max-attempts: 6
        multiplier: 1.1
